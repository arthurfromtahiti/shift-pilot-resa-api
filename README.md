# shift-pilot-resa-api

Pilote de test SHIFT/Paperclip — API de réservation de transferts inter-îles.
Consommée par `shift-pilot-resa-web` (même projet, dépôt séparé).

## Stack
Node 18, `node:test`, aucune dépendance externe.
Tests : `npm test`
